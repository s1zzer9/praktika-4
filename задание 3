#include <iostream>
using namespace std;

int main() {
	setlocale(0, "");
	int  a;
	int  b;

	cout << "Введите вклад рублей: ";
	cin >> a;
	cout << "Введите сколько лет: ";
	cin >> b;

	for (int i = 0; i < b; i++)
	{
		a += a * 0.03;

	}
		
	cout << "Получается: " << a << endl;

	return 0;
}
